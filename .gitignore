.DS_Store
Thumbs.db
/fsh-generated
/input-cache
/output
/temp
/template
env.sh
dump.rdb
resource.json
structuremap.sh
/logs
application.yaml
*.ndjson
.vscode
.idea
.claude
.venv
_build.*
.playwright*
sil-templates/*

# Offline FML compile output. `make ig` writes the real StructureMaps to
# /output; this directory only exists so a single map can be compiled without a
# full publisher run, and it is a throwaway either way.
/input/compiled-maps
*.pdf